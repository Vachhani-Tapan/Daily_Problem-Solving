class Solution {
public:
    bool isPalindrome(int x) {
        long long temp = x;
        long long res = 0;
        for (int i = temp; i > 0; i = i / 10)
        {
            res = (res * 10) + (i % 10);
        }
        return res == x;
    }
};  